diff --git a/shaders/fusion_compact.slang b/shaders/fusion_compact.slang
index 0ed69e4..d4dbdfb 100644
--- a/shaders/fusion_compact.slang
+++ b/shaders/fusion_compact.slang
@@ -4,6 +4,12 @@
 // The legacy graph remains the independent numerical reference.
 #include "guided.slang"
 #include "zcurve.slang"
+#ifndef WAVE_GUIDED
+#define WAVE_GUIDED 0
+#endif
+#ifndef WAVE_REDUCTION
+#define WAVE_REDUCTION 0
+#endif
 #ifndef REDUCTION_GRID
 #define REDUCTION_GRID 4
 #endif
@@ -263,6 +269,31 @@ void reduce_setup_cooperative(uint3 local : SV_GroupThreadID,uint3 group : SV_Gr
     uint w,h;compactOutput.GetDimensions(w,h);
     uint2 pixel=group.xy*uint2(8,8)+uint2(local.x/4,local.y);
     uint column=local.x%4,lane=local.y*32+local.x;
+#if WAVE_REDUCTION
+    // Four consecutive lanes each integrate a 2x2 quad. Validate the mapping
+    // instead of assuming a subgroup width or implementation-specific ordering.
+    uint waveLane=WaveGetLaneIndex();
+    uint first=WaveReadLaneFirst(lane);
+    bool ordered=WaveActiveAllTrue(lane==first+waveLane);
+    uint sw,sh;fullSource.GetDimensions(sw,sh);
+    if(!ordered || (first&3)!=0 || (WaveActiveCountBits(true)&3)!=0 ||
+       WaveGetLaneCount()<4 || sw!=w*4 || sh!=h*4) {
+        if(column==0) reduceSampled(uint3(pixel,0));
+        return;
+    }
+    float2 uv=(float2(pixel)+(float2(column&1,column>>1)+.5)/2.0)/float2(w,h);
+    float4 r=max(fullSource.GatherRed(linearSampler,uv),0.0);
+    float4 g=max(fullSource.GatherGreen(linearSampler,uv),0.0);
+    float4 b=max(fullSource.GatherBlue(linearSampler,uv),0.0);
+    float4 luminance=r*.2126+g*.7152+b*.0722;
+    float2 partial=float2(dot(luminance,float4(1)),dot(log2(max(luminance,1e-6)),float4(1)));
+    uint cluster=waveLane&~3u;
+    float2 total=WaveReadLaneAt(partial,cluster);
+    total+=WaveReadLaneAt(partial,cluster+1);
+    total+=WaveReadLaneAt(partial,cluster+2);
+    total+=WaveReadLaneAt(partial,cluster+3);
+    if(column==0 && pixel.x<w && pixel.y<h) storeReducedLuma(pixel,total.x/16.0,total.y/16.0);
+#else
     float4 sum=0;
     for(uint y=0;y<4;y++) {
         float2 uv=(float2(pixel)+(float2(column,y)+.5)/4.0)/float2(w,h);
@@ -275,6 +306,7 @@ void reduce_setup_cooperative(uint3 local : SV_GroupThreadID,uint3 group : SV_Gr
         sum=((reductionPartials[lane]+reductionPartials[lane+1])+reductionPartials[lane+2])+reductionPartials[lane+3];
         storeReduced(pixel,sum/16.0);
     }
+#endif
 }
 
 Texture2D<float4> fineLuminance;
@@ -469,6 +501,13 @@ void reconstruct_guided(uint3 tid : SV_DispatchThreadID, uint3 localID : SV_Grou
     uint w,h;
     averagedOutput.GetDimensions(w,h);
     uint lane=localID.y*TILE_X+localID.x;
+#if WAVE_GUIDED
+    uint waveLane=WaveGetLaneIndex(),waveCount=WaveGetLaneCount();
+    uint activeCount=WaveActiveCountBits(true);
+    uint firstLane=WaveReadLaneFirst(lane);
+    bool ordered=WaveActiveAllTrue(lane==firstLane+waveLane);
+#endif
+
     int2 origin=int2(groupID.xy*uint2(TILE_X,TILE_Y));
 #if !MOMENT_INPUT
     for(uint i=lane; i<inputPitch*inputHeight; i+=TILE_X*TILE_Y)
@@ -486,11 +525,32 @@ void reconstruct_guided(uint3 tid : SV_DispatchThreadID, uint3 localID : SV_Grou
     // FP32 moments around one tile anchor avoid large absolute-log products.
     // Keep the original 5x5 support; only the arithmetic grouping changes.
     float anchor=fusedInputs[(2*GUIDED_RADIUS)*inputPitch+2*GUIDED_RADIUS].x;
+#if WAVE_GUIDED
+    // Uniform trip counts keep all lanes available to the shuffles.
+    [unroll]
+    for(uint step=0;step<(coefficientPitch*inputHeight+TILE_X*TILE_Y-1)/(TILE_X*TILE_Y);step++)
+    {
+        uint i=step*TILE_X*TILE_Y+lane;
+        uint safeI=min(i,coefficientPitch*inputHeight-1);
+        float2 own=fusedInputs[(safeI/coefficientPitch)*inputPitch+safeI%coefficientPitch];
+        float ownG=own.x-anchor;
+        float4 ownMoment=float4(ownG,own.y,ownG*ownG,ownG*own.y);
+#else
     for(uint i=lane;i<coefficientPitch*inputHeight;i+=TILE_X*TILE_Y)
     {
+#endif
         uint row=i/coefficientPitch, col=i%coefficientPitch;
         float4 moments=0;
         for(uint x=0;x<=2*GUIDED_RADIUS;x++) {
+#if WAVE_GUIDED
+            float4 neighbor=WaveReadLaneAt(ownMoment,min(waveLane+x,waveCount-1));
+            uint safeRow=min(row,inputHeight-1);
+            float2 sample=fusedInputs[safeRow*inputPitch+col+x];
+            float g=sample.x-anchor,e=sample.y;
+            float4 fallback=float4(g,e,g*g,g*e);
+            moments+=ordered && waveLane+x<activeCount && col+x<coefficientPitch && i+x<coefficientPitch*inputHeight
+                ? neighbor : fallback;
+#else
             float2 sample=fusedInputs[row*inputPitch+col+x];
 #if HALF_MOMENT_PRODUCTS
             half g=half(sample.x-anchor),e=half(sample.y);
@@ -498,12 +558,19 @@ void reconstruct_guided(uint3 tid : SV_DispatchThreadID, uint3 localID : SV_Grou
 #else
             float g=sample.x-anchor,e=sample.y;
             moments+=float4(g,e,g*g,g*e);
+#endif
 #endif
         }
+#if WAVE_GUIDED
+        if(i<coefficientPitch*inputHeight) {
+#endif
 #if SHARED_PADDING
         rowMoments[row*momentPitch+col]=moments;
 #else
         rowMoments[i]=moments;
+#endif
+#if WAVE_GUIDED
+        }
 #endif
     }
     GroupMemoryBarrierWithGroupSync();
@@ -550,14 +617,35 @@ void reconstruct_guided(uint3 tid : SV_DispatchThreadID, uint3 localID : SV_Grou
     }
     GroupMemoryBarrierWithGroupSync();
 #if SEPARABLE_GUIDED
+#if WAVE_GUIDED
+    [unroll]
+    for(uint step=0;step<(TILE_X*coefficientHeight+TILE_X*TILE_Y-1)/(TILE_X*TILE_Y);step++) {
+        uint i=step*TILE_X*TILE_Y+lane;
+        uint row=i/TILE_X,col=i%TILE_X;
+        uint safeRow=min(row,coefficientHeight-1);
+        float2 own=fusedCoefficients[safeRow*coefficientPitch+col];
+        float2 sum=0;
+        for(uint x=0;x<=2*GUIDED_RADIUS;x++) {
+            float2 neighbor=WaveReadLaneAt(own,min(waveLane+x,waveCount-1));
+            sum+=ordered && waveLane+x<activeCount && col+x<TILE_X && i+x<TILE_X*coefficientHeight
+                ? neighbor : fusedCoefficients[safeRow*coefficientPitch+col+x];
+        }
+#else
     for(uint i=lane;i<TILE_X*coefficientHeight;i+=TILE_X*TILE_Y) {
         uint row=i/TILE_X,col=i%TILE_X;
         float2 sum=0;
         for(uint x=0;x<=2*GUIDED_RADIUS;x++)sum+=fusedCoefficients[row*coefficientPitch+col+x];
+#endif
+#if WAVE_GUIDED
+        if(i<TILE_X*coefficientHeight) {
+#endif
 #if SHARED_PADDING
         rowCoefficients[row*rowCoefficientPitch+col]=sum;
 #else
         rowCoefficients[i]=sum;
+#endif
+#if WAVE_GUIDED
+        }
 #endif
     }
     GroupMemoryBarrierWithGroupSync();
diff --git a/tools/profiling/android/prepare.py b/tools/profiling/android/prepare.py
index 0c37b48..953d1c4 100644
--- a/tools/profiling/android/prepare.py
+++ b/tools/profiling/android/prepare.py
@@ -114,6 +114,8 @@ def prepare(out, width, height, image, ev=0., source_format='rgba32_float', outp
         if module in ('fusion_compact','present'):
             flags += [f"-DWORK_X={variant_config.get('work_x',8)}",f"-DWORK_Y={variant_config.get('work_y',8)}"]
         if module=='fusion_compact':
+            flags += [f"-DWAVE_GUIDED={int(variant_config.get('wave_guided',False))}"]
+            flags += [f"-DWAVE_REDUCTION={int(variant_config.get('wave_reduction',False))}"]
             flags += [f"-DSHARED_PADDING={variant_config.get('shared_padding',0)}",f"-DHALF_MOMENT_PRODUCTS={int(variant_config.get('half_moment_products',False))}",f"-DREDUCTION_GRID={variant_config.get('reduction_grid',4)}",f"-DGUIDED_RADIUS={variant_config.get('guided_radius',2)}",f"-DCACHED_BASELINE={int(variant_config.get('cached_baseline',False))}",f"-DLOG_EXPOSURE={int(variant_config.get('log_exposure',False))}",f"-DTILE_X={variant_config.get('tile_x',16)}",f"-DTILE_Y={variant_config.get('tile_y',16)}",f"-DSEPARABLE_GUIDED={int(variant_config.get('separable_guided',False))}",f"-DREDUCTION_LOAD={int(variant_config.get('reduction_load',False))}",f"-DREUSE_SHARED={int(variant_config.get('reuse_shared',False))}",f"-DSINGLE_WEIGHT={int(variant_config.get('single_weight',False))}",f"-DPRECOMPUTED_EV={int(variant_config.get('precomputed_ev',False))}",f"-DCURVE_LOG={int(variant_config.get('curve_log',False))}",f"-DRESIDUAL_PYRAMID={int(variant_config.get('residual_pyramid',False))}",f"-DPACKED_RESIDUAL={int(variant_config.get('packed_residual',False))}",f"-DPACKED_WEIGHT_BIAS={variant_config.get('packed_weight_bias',0.0)}",f"-DRESIDUAL_SCALE={variant_config.get('residual_scale',1.0)}",f"-DMOMENT_INPUT={int(variant_config.get('moment_input',False))}",f"-DLOG_PRODUCT={int(variant_config.get('log_product',False))}",f"-DSCALAR_REDUCTION={int(variant_config.get('scalar_reduction',False))}",f"-DHALF_GATHER={int(variant_config.get('half_gather',False))}",f"-DGATHER_X={variant_config.get('gather_x',8)}",f"-DGATHER_Y={variant_config.get('gather_y',8)}"]
         cmd = [str(exe), str(path), '-I',str(ROOT/'shaders'), '-entry', entry, *flags,
                '-target', 'spirv', '-o', str(out/f'{entry}.spv'),
diff --git a/tools/profiling/android/quality_sweep.py b/tools/profiling/android/quality_sweep.py
index 9754009..8fc9dae 100644
--- a/tools/profiling/android/quality_sweep.py
+++ b/tools/profiling/android/quality_sweep.py
@@ -14,7 +14,7 @@ def codes(linear):
 class Candidate:
     def __init__(self,device,mapper,variant):
         self.device,self.mapper,self.config=device,mapper,VARIANTS[variant]
-        mapping={'SHARED_PADDING':('shared_padding',0),'HALF_MOMENT_PRODUCTS':('half_moment_products',False),'GATHER_X':('gather_x',8),'GATHER_Y':('gather_y',8),'HALF_GATHER':('half_gather',False),'SCALAR_REDUCTION':('scalar_reduction',False),'LOG_PRODUCT':('log_product',False),'MOMENT_INPUT':('moment_input',False),'PACKED_RESIDUAL':('packed_residual',False),'RESIDUAL_PYRAMID':('residual_pyramid',False),'CURVE_LOG':('curve_log',False),'PRECOMPUTED_EV':('precomputed_ev',False),'SINGLE_WEIGHT':('single_weight',False),'REUSE_SHARED':('reuse_shared',False),'REDUCTION_LOAD':('reduction_load',False),'REDUCTION_GRID':('reduction_grid',4),'GUIDED_RADIUS':('guided_radius',2),'CACHED_BASELINE':('cached_baseline',False),'LOG_EXPOSURE':('log_exposure',False),'TILE_X':('tile_x',16),'TILE_Y':('tile_y',16),'WORK_X':('work_x',8),'WORK_Y':('work_y',8),'SEPARABLE_GUIDED':('separable_guided',False)}
+        mapping={'WAVE_GUIDED':('wave_guided',False),'WAVE_REDUCTION':('wave_reduction',False),'SHARED_PADDING':('shared_padding',0),'HALF_MOMENT_PRODUCTS':('half_moment_products',False),'GATHER_X':('gather_x',8),'GATHER_Y':('gather_y',8),'HALF_GATHER':('half_gather',False),'SCALAR_REDUCTION':('scalar_reduction',False),'LOG_PRODUCT':('log_product',False),'MOMENT_INPUT':('moment_input',False),'PACKED_RESIDUAL':('packed_residual',False),'RESIDUAL_PYRAMID':('residual_pyramid',False),'CURVE_LOG':('curve_log',False),'PRECOMPUTED_EV':('precomputed_ev',False),'SINGLE_WEIGHT':('single_weight',False),'REUSE_SHARED':('reuse_shared',False),'REDUCTION_LOAD':('reduction_load',False),'REDUCTION_GRID':('reduction_grid',4),'GUIDED_RADIUS':('guided_radius',2),'CACHED_BASELINE':('cached_baseline',False),'LOG_EXPOSURE':('log_exposure',False),'TILE_X':('tile_x',16),'TILE_Y':('tile_y',16),'WORK_X':('work_x',8),'WORK_Y':('work_y',8),'SEPARABLE_GUIDED':('separable_guided',False)}
         defines={key:str(int(self.config.get(name,default))) for key,(name,default) in mapping.items()}
         defines['RESIDUAL_SCALE']=str(self.config.get('residual_scale',1.0))
         defines['PACKED_WEIGHT_BIAS']=str(self.config.get('packed_weight_bias',0.0))
diff --git a/tools/profiling/android/traffic.py b/tools/profiling/android/traffic.py
index 1d11b80..6f6d97b 100644
--- a/tools/profiling/android/traffic.py
+++ b/tools/profiling/android/traffic.py
@@ -57,7 +57,7 @@ def estimate(manifest, fps=45):
             if 'weightsOutput' in descriptors:write('weightsOutput')
             if 'baseLightnessOutput' in descriptors:write('baseLightnessOutput')
         elif entry in ('reduce_setup','reduce_setup_cooperative','reduce_setup_gather'):
-            gather=entry=='reduce_setup_gather' and manifest['width']==4*p['width'] and manifest['height']==4*p['height']
+            gather=(entry=='reduce_setup_gather' or (entry=='reduce_setup_cooperative' and manifest['config'].get('variant_settings',{}).get('wave_reduction'))) and manifest['width']==4*p['width'] and manifest['height']==4*p['height']
             read('fullSource', n*(12 if gather else manifest['config'].get('variant_settings',{}).get('reduction_grid',4)**2), True); write('compactOutput'); write('lightnessOutput')
             if 'weightsOutput' in descriptors:write('weightsOutput')
             if 'baseLightnessOutput' in descriptors:write('baseLightnessOutput')
diff --git a/tools/profiling/android/variants.py b/tools/profiling/android/variants.py
index 89ace32..fa5c981 100644
--- a/tools/profiling/android/variants.py
+++ b/tools/profiling/android/variants.py
@@ -64,3 +64,9 @@ VARIANTS = {
     'radius1': {'guided_radius': 1},
     'mobile': {'half_aux': True, 'reduction_grid': 2, 'guided_radius': 1},
 }
+
+# Wave candidates remain opt-in until matched device measurements pass.
+VARIANTS["wave-reduction"] = {**VARIANTS["gather-reduction"], "cooperative_reduction": True, "wave_reduction": True}
+
+VARIANTS["wave-guided"] = {**VARIANTS["gather-reduction"], "wave_guided": True}
+VARIANTS["wave-both"] = {**VARIANTS["wave-reduction"], "wave_guided": True}
